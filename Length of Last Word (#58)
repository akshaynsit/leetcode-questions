class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        
        k=s.split()
        if len(k)==0:
            return 0
        else:
            return len(k[-1])
